{
  "name": "example-project",
  "version": "1.0.0",
  "description": "Example project for testing Scripts Launcher extension",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "jest",
    "dev": "nodemon src/index.ts",
    "lint": "eslint src --ext .ts",
    "clean": "rimraf dist",
    "watch": "tsc --watch",
    "serve": "http-server dist"
  },
  "devDependencies": {
    "typescript": "^4.9.4",
    "jest": "^29.0.0",
    "nodemon": "^2.0.20",
    "eslint": "^8.28.0",
    "rimraf": "^3.0.2",
    "http-server": "^14.1.1"
  }
}